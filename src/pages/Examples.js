import React from "react";

const Examples = () => {
    return (
        <main className="d-flex justify-content-between">
            <h1>Examples</h1>
        </main>
    );
};

export default Examples;